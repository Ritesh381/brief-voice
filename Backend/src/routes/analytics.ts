// Backend/src/routes/analytics.ts
// Analytics routes — implemented in Checkpoint 3.

import { FastifyInstance } from "fastify";

export default async function analyticsRoutes(_app: FastifyInstance) {
  // Routes registered in Checkpoint 3.
}
