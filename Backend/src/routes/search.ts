// Backend/src/routes/search.ts
// Semantic search routes — implemented in Checkpoint 4 (RAG).

import { FastifyInstance } from "fastify";

export default async function searchRoutes(_app: FastifyInstance) {
  // Routes registered in Checkpoint 4.
}
